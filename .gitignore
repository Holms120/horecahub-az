# Dependencies
node_modules/

# Build output
dist/

# Environment variables — NEVER commit these
.env
.env.local
.env.*.local

# Editor & OS
.DS_Store
.vscode/
*.log
.claude/

# Local-only Claude notes (may contain deploy hooks / secrets — never commit)
CLAUDE.local.md

# Supabase local CLI state
supabase/.temp/
.env
deno.lock
.vercel
